# Blue-Green Upstream Configuration - GREEN
# Copy this to upstream.conf to activate GREEN deployment

# Events Application - GREEN (Standby)
upstream events_app {
    server events-green:3000;
    keepalive 32;
}

# Store Application - GREEN (Standby)
upstream store_app {
    server store-green:3000;
    keepalive 32;
}
