# Blue-Green Upstream Configuration - BLUE
# Copy this to upstream.conf to activate BLUE deployment

# Events Application - BLUE
upstream events_app {
    server events-blue:3000;
    keepalive 32;
}

# Store Application - BLUE
upstream store_app {
    server store-blue:3000;
    keepalive 32;
}
