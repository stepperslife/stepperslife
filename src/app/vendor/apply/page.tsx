import VendorApplyClient from "./VendorApplyClient";

export const metadata = {
  title: "Apply to Sell - SteppersLife Marketplace",
  description: "Apply to become a vendor on SteppersLife and start selling your products to the stepping community.",
};

export default function VendorApplyPage() {
  return <VendorApplyClient />;
}
